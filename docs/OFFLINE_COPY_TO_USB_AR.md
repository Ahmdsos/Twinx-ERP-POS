# 💾 نسخ Twinx ERP على فلاشة USB — دليل التوزيع

> هذا الدليل يشرح كيفية تجهيز نسخة من المشروع للتوزيع عبر USB (فلاشة).

---

## 🎯 طريقتان للتوزيع

| الطريقة | الحجم التقريبي | المميزات | العيوب |
|---|---|---|---|
| **A — كاملة (Offline)** | ~100-200 MB | تعمل بدون إنترنت | حجم أكبر |
| **B — خفيفة (Lean)** | ~20-40 MB | حجم صغير | تحتاج إنترنت لتثبيت المتطلبات |

---

## الطريقة A — نسخة كاملة (بدون إنترنت)

### الخطوة 1: تشغيل سكربت الحزمة
```
tools\make_usb_package.bat A
```

### الخطوة 2: النتيجة
- 📁 مجلد `dist_usb\` يحتوي على المشروع الكامل
- 📦 ملف `dist_usb.zip` جاهز للنقل

### الخطوة 3: على الجهاز الجديد
1. ثبّت XAMPP
2. فك ضغط `dist_usb.zip` في `C:\xampp\htdocs\twinx-erp`
3. شغّل: `tools\install_to_xampp.bat`

---

## الطريقة B — نسخة خفيفة (تحتاج إنترنت)

### الخطوة 1: تشغيل سكربت الحزمة
```
tools\make_usb_package.bat B
```

### الخطوة 2: على الجهاز الجديد
1. ثبّت XAMPP + Composer + Node.js
2. فك ضغط الملف في `C:\xampp\htdocs\twinx-erp`
3. شغّل: `tools\install_to_xampp.bat`
4. السكربت سيقوم تلقائيًا بتثبيت المتطلبات

---

## 📋 محتويات الحزمة

### ما يتم تضمينه ✅
- كود المشروع بالكامل (app, Modules, resources, routes, config)
- ملفات الإعداد (.env.xampp.example, .env.example)
- سكربتات الأدوات (tools/*.bat)
- الوثائق (docs/*)
- أصول الواجهة المبنية (public/build/) — في الطريقة A
- حزم PHP (vendor/) — في الطريقة A فقط

### ما يتم استبعاده ❌
- ملفات Git (.git/)
- ملفات التطوير (node_modules/, tests/)
- قواعد البيانات المحلية (database.sqlite)
- ملفات السجلات (*.log)
- سكربتات التصحيح المؤقتة
- ملف الترخيص الشخصي (.license.key)

---

## 🔧 نصائح

- **تأكد من بناء الواجهة** قبل عمل الحزمة:
  ```
  npm run build
  ```
- **افحص المتطلبات** على الجهاز الجديد:
  ```
  tools\check_requirements.bat
  ```
- **حجم الفلاشة المطلوب**: 512 MB كحد أدنى (للطريقة A)
